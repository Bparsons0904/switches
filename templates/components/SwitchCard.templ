package components

import "fmt"

type SwitchCardProps struct {
	ID          string
	Title       string
	Description string
	ImageSrc    string
	ImageAlt    string
}

templ SwitchCard(props SwitchCardProps) {
	<a
		class="switch-card"
		hx-get="switches/modal"
		hx-swap="innerHTML"
		hx-target="#portal"
		hx-params={ fmt.Sprintf(`{"switchId": "%s"}`, props.ID) }
	>
		<div class="switch-image">
			@ImageLink(ImageProps{
				Src:      props.ImageSrc,
				Alt:      props.ImageSrc,
				IsSquare: true,
			})
		</div>
		<div class="switch-details">
			<h2>{ props.Title }</h2>
			<p>{ props.Description }</p>
		</div>
	</a>
}
