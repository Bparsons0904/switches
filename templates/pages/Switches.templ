
package pages

import (
	"switches/models"
	"switches/templates/components"
	"switches/templates/layouts"
)

templ SwitchesPage(user models.User) {
	@layouts.Root() {
		@Switches(user)
	}
}

templ Switches(user models.User) {
	<div class="">
		@components.Navbar("switches", models.User{})
		<main id="home">
			<section id="cta" class="dark">Yo</section>
		</main>
	</div>
}
